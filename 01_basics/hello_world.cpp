#include <iostream>

int main() // main function
{
    // std - standard namespace
    // we specify namespace in order to avoid conficts of function naming
    std::cout << "Hello world!\n";
    return 0;
}